version: '3.8'
services:
  postgres-db:
    image: postgres:16
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: adminpassword
      POSTGRES_DB: saloondb

    ports:
      - "5432:5432"

    volumes:
      - pgdata:/var/lib/postgresql/data


volumes:
  pgdata:
